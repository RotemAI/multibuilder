Scoped security review, 2026-09-07.

No new high-severity finding. Creation remains authenticated and account-bound. Model and effort must appear together in the verified account catalog before any tmux creation. Launch arguments use the existing quoted command builder. The new fields are stored inside the private lifecycle record for that exact owner and generation, and updates compare the original settings atomically. No credential, provider, sandbox or permission override is added by this change.

Recovery reads only the validated root transcript through the existing no-symlink file opener, capped at one MB. It reads structured terminal turn events, never user or assistant prose. Cyber, misalignment, authorization, sandbox, usage and budget errors are excluded. A narrow fallback for Codex's observed `other` HTTP 503 representation requires the exact transport prefix and an operational status. Recovery waits for a completed failed turn and an empty live composer, verifies the immutable tmux binding and session generation, uses the same root with fresh-session recovery disabled, and confirms submission. Closed sessions, drafts, active turns and approval menus prevent terminal input. It is limited to subscription sessions and two model switches per unsuccessful episode. No new metered API route or dependency is introduced.

The interface escapes catalog labels, model IDs and names; recovery notices use textContent. Existing authentication cookies and CSP, frame and content-type headers remain. The dependency inventory was refreshed: existing updates are available for FastAPI, Starlette, Pydantic, Google Auth, OpenAI, uvicorn, websockets and setuptools. This patch does not change dependencies. Existing server version headers remain a low-severity maintenance item.

Only source changes, synthetic regression tests and the deployment bundle are reviewable files. Browser cookies, real transcript captures, account data and local outage probe state are excluded.
