# Pinned 2026-07-28.
fastapi==0.140.7          # supports Starlette 1.x security fixes
uvicorn==0.39.0           # updated 2026-04-03 (was 0.34.0)
openai==1.58.1            # latest: 2.30.0 (MAJOR — breaking API changes; skip until manual review)
python-multipart==0.0.32  # fixes 2026 parser-smuggling, file-write, and DoS advisories
starlette==1.3.1          # fixes 2026 Host validation, disclosure, and DoS advisories
pydantic==2.12.5          # updated 2026-04-03 (was 2.10.3)
httpx==0.28.1             # browser/noVNC reverse proxy
websockets==15.0.1        # browser WebSocket reverse proxy
google-auth[requests]==2.56.2  # Workspace domain-wide delegation preflight
cryptography==50.0.1      # AES-256-GCM at-rest encryption for stored SSH credentials
tomli==2.4.1; python_version < "3.11"  # stdlib tomllib compatibility
psycopg[binary]==3.2.3   # PostgreSQL driver for shared team state
psycopg-pool==3.2.4      # connection pooling for the dashboard workers
