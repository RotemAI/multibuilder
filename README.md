# MultiBuilder end-to-end canary

This private fixture proves the production scheduler's complete coding path.

The requested implementation creates `greeting.txt` with exactly this single line:

```text
MultiBuilder vertical slice complete
```

The checked-in validation contract runs the same deterministic assertion through every required gate.
